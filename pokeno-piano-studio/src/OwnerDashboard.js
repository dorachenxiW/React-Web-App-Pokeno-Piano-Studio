
const OwnerDashboard = ({ name }) => {
    return (
        <div>
            <h3>Welcome to Owner Dashboard, {name}</h3>
            {/* Add content specific to the owner dashboard */}
        </div>
    );
}

export default OwnerDashboard;
